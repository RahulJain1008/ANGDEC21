import { HlGlowDirective } from './hl-glow.directive';

describe('HlGlowDirective', () => {
  it('should create an instance', () => {
    const directive = new HlGlowDirective();
    expect(directive).toBeTruthy();
  });
});
